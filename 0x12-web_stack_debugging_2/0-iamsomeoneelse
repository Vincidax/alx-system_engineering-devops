#!/usr/bin/env bash

# Check if a username is provided as an argument
if [ $# -ne 1 ]; then
    echo "Usage: $0 <username>"
    exit 1
fi

# Run whoami under the provided username
sudo -u "$1" whoami
